# Widen Mob AI goal selectors from protected to public.
# NeoForge already does this at runtime; this AT is needed for compile-time
# in the common module which uses vanilla-mode NeoForm (no NeoForge patches).
public net.minecraft.world.entity.Mob goalSelector
public net.minecraft.world.entity.Mob targetSelector
