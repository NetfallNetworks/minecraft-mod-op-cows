# .github/workflows/build.yml
name: Build

on:
  push:
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build and Test
        run: ./gradlew build --no-configuration-cache

      - name: Print Test Failures
        if: failure()
        run: |
          echo "=== Test Results ==="
          find build/reports/tests -name "*.xml" -path "*/TEST-*" -exec echo "--- {} ---" \; -exec cat {} \; 2>/dev/null || true
          echo "=== Test Output ==="
          find build/test-results -name "*.xml" -exec echo "--- {} ---" \; -exec cat {} \; 2>/dev/null || true
          echo "=== Build Output ==="
          find build/reports -name "*.html" -exec echo "--- {} ---" \; 2>/dev/null || true

      - name: Upload Test Report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-report-${{ github.sha }}
          path: build/reports/tests/

      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: mooofdoom-${{ github.sha }}
          path: build/libs/*.jar
          if-no-files-found: error
